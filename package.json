{
  "name": "ig-local-autopost",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "install:browsers": "playwright install chromium",
    "install:browsers:linux": "playwright install --with-deps chromium"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "1.4.5-lts.1",
    "playwright": "^1.43.0"
  }
}
