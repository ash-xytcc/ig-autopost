IG Autopost local app

Run:
1. npm install
2. npx playwright install
3. npm start
4. open http://localhost:3000

Notes:
- Add account opens a browser. Log in to Instagram, then close that browser window.
- Scheduler checks every 15 seconds.
- Runtime files are ignored by git.
- Debug screenshots save into uploads/ when posting fails.
